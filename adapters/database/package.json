{
  "name": "pointchu.database-adapter",
  "version": "0.0.1",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "devDependencies": {},
  "dependencies": {
    "pointchu.capabilities": "workspace:*",
    "pointchu.common-config": "workspace:*",
    "pointchu.domain": "workspace:*",
    "pointchu.use-cases": "workspace:*",
    "firebase": "9.22.1"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "engines": {
    "node": ">=16"
  }
}
